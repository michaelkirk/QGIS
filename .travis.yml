language: cpp

compiler:
  - clang

cache: apt

os:
  - linux
  - osx

before_install:
  - tests/scripts/travis/setup.sh

install:
  - tests/scripts/travis/build.sh

script:
  - tests/scripts/travis/run_tests.sh

